# Test Instance
[invenio-dev01.tugraz.at](https://invenio-dev01.tugraz.at/)

The <env_placeholder> instance is deployed in a Kubernetes cluster, which runs on <count> nodes (Debian VMs).

More information about the Kubernetes deployment [here](base.md).
